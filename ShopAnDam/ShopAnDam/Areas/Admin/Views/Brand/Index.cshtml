@model ShopAnDam.Models.Framework.Brand
@{
    ViewBag.Title = "Thương hiệu";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section header{
    Danh sách thương hiệu
}

@section jsFooter{
    <script src="~/Assets/admin/js/controller/Brands.js"></script>
   
}
<div class="row mt">

    <div class="form-group col-lg-12">
        <div class="col-md-8">
            <button  type="button" class="btn btn-primary" data-toggle="modal" data-target="#myModal" onclick="clearTextBox();"><i class="fa fa-plus"></i>Thêm mới</a></button>
        </div>
        <div class="input-append col-md-3  ">
            <input type="text" class="form-control " id="Search"  name="searchString" placeholder="Nhập từ khóa cần tìm">
            <span class="input-group-btn add-on">
                <button class="btn btn-theme" type="submit"><i class="fa fa-search"></i></button>
            </span>
        </div>
    </div>
    <div class="col-md-12">
        <div class="content-panel">
            <table class="table table-striped table-advance table-hover">
                <thead>
                    <tr class="Search" >
                        <th><i class="fa fa-bullhorn"></i> Mã thương hiệu</th>
                        <th><i class="fa fa-bookmark"></i> Tên thương hiệu</th>
                        <th><i class="fa fa-bookmark"></i> Logo</th>
                        <th><i class=" fa fa-edit"></i> Hành động</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody class="tbody">

                    @*@foreach (var item in Model)
                        {
                            <tr id="row_@item.ID">
                                <td>@item.UserName</td>
                                <td>@item.Name</td>
                                <td>@item.Phone</td>
                                <td>@item.Email </td>
                                <td><a href="#" class="label label-success label-mini btn-active" data-id="@item.ID">@(item.Status ?"Kích hoạt": "Khóa")</a></td>
                                <td>
                                    <button class="btn btn-success btn-xs"><i class="fa fa-check"></i></button>
                                    <a href="/Admin/User/Edit/@item.ID" class="btn btn-primary btn-xs"><i class="fa fa-pencil"></i></a>
                                    @Ajax.ActionLink(" ", "Delete", new { @id = item.ID }, new AjaxOptions
                               {
                                   Confirm = "Bạn có muốn xóa bản ghi này?",
                                   OnComplete = "$('#row_" + @item.ID + "').remove()",
                                   HttpMethod = "Delete"
                               }, new { @class = "btn btn-danger btn-xs fa fa-trash-o " }
                                   )
                                </td>
                            </tr>
                        }*@
                </tbody>
            </table>

        </div>
        <!-- /content-panel -->

        <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        @*<button type="button" class="close" data-dissmiss="modal"><span aria-hidden="true">&times;</span></button>*@
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title" id="myModalLabel">Add Brand</h4>
                    </div>
                    <div class="modal-body">
                        <form>
                            <div class="form-group">
                                <label for="Id">ID</label>
                                <input type="text" class="form-control" id="ID" placeholder="ID" disabled="disabled" />
                            </div>
                            <div class="form-group">
                                <label for="Name">Name</label>
                                <input type="text" class="form-control" id="Name" placeholder="Name" />
                            </div>
                            <div class="form-group">
                                <label for="Logo">Logo</label>
                                <input type="text" class="form-control" id="Logo" placeholder="Logo" />
                            </div>
                        </form>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-primary" id="btnAdd" onclick="return Add();"><i class="fa fa-plus"></i>Add</button>
                        <button type="button" class="btn btn-primary" id="btnUpdate" style="display:none;" onclick="Update();">Update</button>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- /col-md-12 -->
</div>
